# CampusEvent

Campus event management and registration system. This repo is the first skeleton. Auth, roles, CRUD, relation management, and deployment docs will be added in later parts.

## Structure
- backend/
- frontend/
- MAP/

## Environment
- Backend: copy `backend/.env.example` to `backend/.env`
- Frontend: copy `frontend/.env.example` to `frontend/.env`

## Health Check
- Backend: `GET /health`

## Commands
See `COMMANDS.md` for copy-paste commands.
